---
// src/components/Navbar.astro

// Detectar la página actual
const currentPath = Astro.url.pathname;
const isHomePage = currentPath === '/' || currentPath === '';
---

<!-- Banner superior estilo eco.com -->
<div class="top-banner">
    <div class="max-w-7xl mx-auto px-6 py-3">
        <div class="flex items-center justify-between">
            <div class="flex items-center space-x-4 text-sm text-white/80">
                <span>🚀 Cuarahí Staff: Estamos en busca de talento para ser mentores de nuestra comunidad.</span>
                <a href="#espacios" class="banner-link">Postula Ahora</a>
            </div>
            <button class="banner-close-btn" aria-label="Cerrar banner">
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
                </svg>
            </button>
        </div>
    </div>
</div>

<nav class="navbar-eco">
    <div class="navbar-container">
        <!-- Enlaces externos izquierda (ocultos en móvil) -->
        <div class="external-links-left">
            <a href="/trabajos" class="external-link">
                <span>TRABAJOS</span>
                <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"></path>
                </svg>
            </a>
        </div>

        <!-- Navbar central con glassmorphism -->
        <div class="navbar-center">
            <!-- Logo -->
            <a href="/" class="logo-container">
                <div class="logo-glass-nav">
                    <img src="/assets/logo-blanco.webp" alt="Cuarahí Logo" class="logo-img-nav" />
                </div>
                <span class="brand-text-nav">cuarahí</span>
            </a>
            
            <!-- Separador visual -->
            <div class="nav-separator"></div>
            
            <!-- Navegación principal -->
            <div class="nav-links-desktop">
                <a href="/espacios" class="nav-link">Espacios</a>
                <a href="/blog" class="nav-link">Blog</a>
                <a href="/soporte" class="nav-link">Soporte</a>
                <a href="/contacto" class="nav-link">Contacto</a>
            </div>
        </div>

        <!-- Enlaces externos derecha -->
        <div class="external-links-right">
            <a href="/discord" class="external-link">
                <span>DISCORD</span>
                <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"></path>
                </svg>
            </a>
            <a href="/twitter" class="external-link">
                <span>X</span>
                <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"></path>
                </svg>
            </a>
        </div>

        <!-- Botón menú móvil -->
        <button id="mobile-menu-button" class="mobile-menu-btn">
            <span class="hamburger-line"></span>
            <span class="hamburger-line"></span>
            <span class="hamburger-line"></span>
        </button>
    </div>

    <!-- Menú móvil -->
    <div id="mobile-menu" class="mobile-menu">
        <div class="mobile-menu-content">
            <div class="mobile-nav-links">
                <a href="/espacios" class="mobile-nav-link">Espacios</a>
                <a href="/blog" class="mobile-nav-link">Blog</a>
                <a href="/soporte" class="mobile-nav-link">Soporte</a>
                <a href="/contacto" class="mobile-nav-link">Contacto</a>
            </div>
            
            <div class="mobile-external-links">
                <a href="/trabajos" class="mobile-external-link">TRABAJOS ↗</a>
                <a href="/discord" class="mobile-external-link">DISCORD ↗</a>
                <a href="/twitter" class="mobile-external-link">X ↗</a>
            </div>
        </div>
    </div>
</nav>

<style>
/* ===== VARIABLES ESTILO ECO ===== */
:root {
    --navbar-height: 64px;
    --banner-height: 44px;
    --eco-glass: rgba(255, 255, 255, 0.08);
    --eco-glass-border: rgba(255, 255, 255, 0.12);
    --eco-glass-hover: rgba(255, 255, 255, 0.12);
    --cuarahi-blue: #1E40AF;
    --cuarahi-blue-light: #3B82F6;
    --cuarahi-red: #DC2626;
    --cuarahi-red-light: #EF4444;
    --cuarahi-cyan: #0EA5E9;
    --text-primary: #ffffff;
    --text-secondary: rgba(255, 255, 255, 0.8);
    --text-tertiary: rgba(255, 255, 255, 0.6);
}

/* ===== BANNER SUPERIOR ===== */
.top-banner {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 1001;
    background: linear-gradient(135deg, #0f1419 0%, #1a2332 100%);
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
}

.banner-link {
    color: var(--cuarahi-cyan);
    text-decoration: underline;
    transition: all 0.2s ease;
}

.banner-link:hover {
    color: var(--cuarahi-blue-light);
    text-decoration: none;
}

.banner-close-btn {
    color: rgba(255, 255, 255, 0.6);
    transition: all 0.2s ease;
    padding: 4px;
    border-radius: 4px;
}

.banner-close-btn:hover {
    color: white;
    background: rgba(255, 255, 255, 0.1);
}

/* ===== NAVBAR PRINCIPAL ===== */
.navbar-eco {
    position: fixed;
    top: calc(var(--banner-height) + 12px); /* Agrega 12px de separación */
    left: 0;
    right: 0;
    z-index: 1000;
    background: transparent;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    height: var(--navbar-height);
    display: flex;
    align-items: center;
    justify-content: center;
}

.navbar-container {
    max-width: 1400px;
    width: 100%;
    height: 100%; /* Cambiado a 100% para ocupar todo el alto de la navbar */
    margin: 0 auto;
    padding: 0 4rem; /* Solo padding horizontal */
    display: grid;
    grid-template-columns: 1fr auto 1fr;
    align-items: center;
    gap: 1rem;
}

.navbar-center {
    display: flex;
    align-items: center;
    gap: 1rem;
    background: var(--eco-glass);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    border: 1px solid var(--eco-glass-border);
    border-radius: 50px;
    padding: 0.5rem 1.5rem; /* Reduce el padding vertical */
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    justify-self: center;
    height: 52px; /* Opcional: fuerza un alto máximo para evitar desbordes */
    box-sizing: border-box;
}

/* ===== ENLACES EXTERNOS ===== */
.external-links-left,
.external-links-right {
    display: flex;
    align-items: center;
    gap: 1.5rem;
}

.external-links-left {
    justify-content: flex-start;
}

.external-links-right {
    justify-content: flex-end;
}

.external-link {
    display: flex;
    align-items: center;
    gap: 0.375rem;
    font-size: 0.75rem;
    font-weight: 600;
    letter-spacing: 0.1em;
    color: var(--text-tertiary);
    transition: all 0.2s ease;
    text-decoration: none;
    padding: 0.5rem 0.75rem;
    border-radius: 6px;
}

.external-link:hover {
    color: var(--cuarahi-blue-light);
    background: rgba(255, 255, 255, 0.05);
}

/* ===== NAVBAR CENTRAL ===== */

.navbar-center:hover {
    background: var(--eco-glass-hover);
    border-color: rgba(255, 255, 255, 0.2);
    box-shadow: 0 12px 40px rgba(0, 0, 0, 0.4);
}

/* ===== LOGO NAVBAR ===== */
.logo-container {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    text-decoration: none;
    transition: all 0.2s ease;
}

.logo-container:hover {
    transform: scale(1.02);
}

.logo-container:hover .logo-glass-nav {
    background: linear-gradient(135deg, 
        rgba(30, 64, 175, 0.25) 0%,
        rgba(220, 38, 38, 0.25) 50%,
        rgba(14, 165, 233, 0.25) 100%
    );
    border-color: rgba(255, 255, 255, 0.3);
    box-shadow: 
        0 6px 25px rgba(0, 0, 0, 0.3),
        0 0 30px rgba(30, 64, 175, 0.2),
        inset 0 1px 0 rgba(255, 255, 255, 0.2);
    transform: translateY(-1px);
}

.logo-container:hover .logo-img-nav {
    filter: drop-shadow(0 0 15px rgba(255, 255, 255, 0.8)) 
            drop-shadow(0 0 30px rgba(30, 64, 175, 0.6))
            drop-shadow(0 0 40px rgba(14, 165, 233, 0.4));
    transform: scale(1.05);
}

.logo-glass-nav::before {
    content: '';
    position: absolute;
    inset: 0;
    background: conic-gradient(from 0deg,
        rgba(30, 64, 175, 0.3) 0deg,
        rgba(220, 38, 38, 0.3) 120deg,
        rgba(14, 165, 233, 0.3) 240deg,
        rgba(30, 64, 175, 0.3) 360deg
    );
    border-radius: 10px;
    filter: blur(8px);
    opacity: 0;
    transition: opacity 0.3s ease;
    z-index: 1;
}

.logo-container:hover .logo-glass-nav::before {
    opacity: 0.6;
}

.logo-glass-nav {
    width: 32px;
    height: 32px;
    border-radius: 10px;
    background: linear-gradient(135deg, 
        rgba(30, 64, 175, 0.15) 0%,
        rgba(220, 38, 38, 0.15) 50%,
        rgba(14, 165, 233, 0.15) 100%
    );
    border: 1px solid rgba(255, 255, 255, 0.2);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    position: relative;
    overflow: hidden;
    box-shadow: 
        0 4px 15px rgba(0, 0, 0, 0.2),
        inset 0 1px 0 rgba(255, 255, 255, 0.15);
}

.logo-img-nav {
    width: 20px;
    height: 20px;
    object-fit: contain;
    filter: drop-shadow(0 0 10px rgba(255, 255, 255, 0.6)) 
            drop-shadow(0 0 20px rgba(30, 64, 175, 0.4));
    transition: all 0.3s ease;
    position: relative;
    z-index: 2;
}

.brand-text-nav {
    font-size: 1.125rem;
    font-weight: 700;
    color: var(--text-primary);
    letter-spacing: -0.025em;
}

/* ===== SEPARADOR ===== */
.nav-separator {
    width: 1px;
    height: 24px;
    background: rgba(255, 255, 255, 0.2);
    margin: 0 0.5rem;
}

/* ===== NAVEGACIÓN PRINCIPAL ===== */
.nav-links-desktop {
    display: flex;
    align-items: center;
    gap: 1.5rem;
}

.nav-link {
    font-size: 0.875rem;
    font-weight: 500;
    color: var(--text-secondary);
    text-decoration: none;
    transition: all 0.2s ease;
    padding: 0.5rem 0.75rem;
    border-radius: 8px;
    position: relative;
}

.nav-link:hover {
    color: var(--text-primary);
    background: rgba(255, 255, 255, 0.1);
}

.nav-link--cta {
    background: rgba(255, 255, 255, 0.12);
    color: var(--text-primary);
    font-weight: 600;
    padding: 0.45rem 0.9rem; /* Más pequeño que antes */
    border-radius: 20px;      /* Más pequeño que antes */
    font-size: 0.95rem;       /* Ligeramente más pequeño */
    border: 1px solid rgba(255, 255, 255, 0.2);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    box-shadow: 
        0 4px 20px rgba(0, 0, 0, 0.2),
        inset 0 1px 0 rgba(255, 255, 255, 0.1);
    position: relative;
    overflow: hidden;
}

.nav-link--cta::before {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, 
        rgba(30, 64, 175, 0.1) 0%, 
        rgba(220, 38, 38, 0.1) 100%);
    opacity: 0;
    transition: opacity 0.3s ease;
    border-radius: 25px;
}

.nav-link--cta:hover {
    background: rgba(255, 255, 255, 0.18);
    border-color: rgba(255, 255, 255, 0.3);
    transform: translateY(-1px);
    box-shadow: 
        0 6px 25px rgba(0, 0, 0, 0.3),
        0 0 0 1px rgba(30, 64, 175, 0.2),
        inset 0 1px 0 rgba(255, 255, 255, 0.15);
}

.nav-link--cta:hover::before {
    opacity: 1;
}


/* ===== MENÚ MÓVIL ===== */
.mobile-menu-btn {
    display: none;
    flex-direction: column;
    gap: 4px;
    padding: 8px;
    border: none;
    background: var(--eco-glass);
    border-radius: 8px;
    cursor: pointer;
    transition: all 0.2s ease;
}

.hamburger-line {
    width: 20px;
    height: 2px;
    background: white;
    border-radius: 2px;
    transition: all 0.3s ease;
}

.mobile-menu {
    display: none;
    position: absolute;
    top: 100%;
    left: 0;
    right: 0;
    background: rgba(15, 20, 25, 0.98);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    border-top: 1px solid rgba(255, 255, 255, 0.1);
}

.mobile-menu-content {
    max-width: 1400px;
    margin: 0 auto;
    padding: 2rem;
}

.mobile-nav-links {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    margin-bottom: 2rem;
}

.mobile-nav-link {
    font-size: 1.125rem;
    font-weight: 500;
    color: var(--text-secondary);
    text-decoration: none;
    padding: 1rem;
    border-radius: 12px;
    transition: all 0.2s ease;
}

.mobile-nav-link:hover {
    color: var(--text-primary);
    background: rgba(255, 255, 255, 0.05);
}

.mobile-nav-link--cta {
    background: linear-gradient(135deg, var(--cuarahi-blue), var(--cuarahi-red));
    color: white;
    font-weight: 600;
    text-align: center;
}

.mobile-external-links {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    padding-top: 1rem;
    border-top: 1px solid rgba(255, 255, 255, 0.1);
}

.mobile-external-link {
    font-size: 0.875rem;
    font-weight: 600;
    color: var(--text-tertiary);
    text-decoration: none;
    padding: 0.75rem;
    transition: all 0.2s ease;
}

.mobile-external-link:hover {
    color: var(--cuarahi-blue-light);
}

/* ===== ESTADOS DE SCROLL ===== */
.navbar-scrolled {
    background: rgba(15, 20, 25, 0.9);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
}

.navbar-scrolled .navbar-center {
    background: var(--eco-glass-hover);
    border-color: rgba(255, 255, 255, 0.15);
}

/* ===== ANIMACIONES ===== */
@keyframes slideDown {
    0% {
        opacity: 0;
        transform: translateY(-20px);
    }
    100% {
        opacity: 1;
        transform: translateY(0);
    }
}

.animate-slide-down {
    animation: slideDown 0.3s ease-out;
}

/* ===== RESPONSIVE DESIGN ===== */
@media (max-width: 1024px) {
    .external-links-left {
        display: none;
    }
    
    .navbar-container {
        grid-template-columns: auto 1fr;
        gap: 1rem;
    }
    
    .external-links-right {
        gap: 1rem;
    }
}

@media (max-width: 768px) {
    .top-banner {
        display: none;
    }
    
    .navbar-eco {
        top: 0;
    }
    
    .navbar-container {
        display: flex;
        justify-content: center;
        align-items: center;
        padding: 0 1rem;
        position: relative;
    }
    
    .external-links-right,
    .nav-links-desktop {
        display: none;
    }
    
    .mobile-menu-btn {
        display: flex;
        position: absolute;
        right: 1rem;
    }
    
    .navbar-center {
        padding: 0.5rem 1rem;
        gap: 0.75rem;
    }
    
    .nav-separator {
        display: none;
    }
    
    .mobile-menu.show {
        display: block;
    }
}

@media (max-width: 640px) {
    .navbar-container {
        padding: 0 1rem;
    }
    
    .mobile-menu-content {
        padding: 1.5rem 1rem;
    }
    
    .brand-text-nav {
        font-size: 1rem;
    }
    
    .logo-glass-nav {
        width: 28px;
        height: 28px;
    }
    
    .logo-img-nav {
        width: 18px;
        height: 18px;
    }
}

/* ===== ACCESIBILIDAD ===== */
@media (prefers-reduced-motion: reduce) {
    * {
        animation-duration: 0.01ms !important;
        animation-iteration-count: 1 !important;
        transition-duration: 0.01ms !important;
    }
}

/* ===== FOCUS STATES ===== */
.nav-link:focus,
.external-link:focus,
.mobile-nav-link:focus {
    outline: 2px solid var(--cuarahi-blue);
    outline-offset: 2px;
}
</style>

<script>
document.addEventListener('DOMContentLoaded', function() {
    const mobileMenuButton = document.getElementById('mobile-menu-button');
    const mobileMenu = document.getElementById('mobile-menu');
    const navbar = document.querySelector('.navbar-eco') as HTMLElement | null;
    const hamburgerLines = document.querySelectorAll('.hamburger-line');
    let isOpen = false;

    // Mobile menu toggle
    if (mobileMenuButton && mobileMenu) {
        mobileMenuButton.addEventListener('click', function() {
            isOpen = !isOpen;
            mobileMenuButton.setAttribute('aria-expanded', String(isOpen));

            if (isOpen) {
                mobileMenu.classList.add('show', 'animate-slide-down');
                // Animar hamburguesa a X
                if (hamburgerLines[0]) (hamburgerLines[0] as HTMLElement).style.transform = 'rotate(45deg) translate(6px, 6px)';
                if (hamburgerLines[1]) (hamburgerLines[1] as HTMLElement).style.opacity = '0';
                if (hamburgerLines[2]) (hamburgerLines[2] as HTMLElement).style.transform = 'rotate(-45deg) translate(6px, -6px)';
            } else {
                mobileMenu.classList.remove('show', 'animate-slide-down');
                // Restaurar hamburguesa
                hamburgerLines.forEach(line => {
                    const element = line as HTMLElement;
                    if (element) {
                        element.style.transform = '';
                        element.style.opacity = '';
                    }
                });
            }
        });

        // Cerrar menú móvil al hacer clic en enlaces
        const mobileLinks = mobileMenu.querySelectorAll('a');
        mobileLinks.forEach(link => {
            link.addEventListener('click', function() {
                isOpen = false;
                mobileMenuButton.setAttribute('aria-expanded', 'false');
                mobileMenu.classList.remove('show', 'animate-slide-down');
                hamburgerLines.forEach(line => {
                    const element = line as HTMLElement;
                    if (element) {
                        element.style.transform = '';
                        element.style.opacity = '';
                    }
                });
            });
        });

        // Cerrar menú móvil al hacer clic fuera
        document.addEventListener('click', (e) => {
            const target = e.target as Node;
            if (!mobileMenuButton.contains(target) && !mobileMenu.contains(target)) {
                if (isOpen) {
                    isOpen = false;
                    mobileMenuButton.setAttribute('aria-expanded', 'false');
                    mobileMenu.classList.remove('show', 'animate-slide-down');
                    hamburgerLines.forEach(line => {
                        const element = line as HTMLElement;
                        if (element) {
                            element.style.transform = '';
                            element.style.opacity = '';
                        }
                    });
                }
            }
        });
    }

    // Navbar scroll effect
    let scrolled = false;
    
    function updateNavbar() {
        const shouldBeScrolled = window.scrollY > 100;
        
        if (shouldBeScrolled !== scrolled) {
            scrolled = shouldBeScrolled;
            
            if (scrolled) {
                navbar?.classList.add('navbar-scrolled');
            } else {
                navbar?.classList.remove('navbar-scrolled');
            }
        }
    }

    // Throttled scroll listener
    let ticking = false;
    
    function onScroll() {
        if (!ticking) {
            requestAnimationFrame(() => {
                updateNavbar();
                ticking = false;
            });
            ticking = true;
        }
    }
    
    window.addEventListener('scroll', onScroll, { passive: true });
    
    // Inicializar estado
    updateNavbar();

    // Banner close functionality
    const bannerCloseBtn = document.querySelector('.banner-close-btn');
    const banner = document.querySelector('.top-banner') as HTMLElement | null;
    
    if (bannerCloseBtn && banner && navbar) {
        bannerCloseBtn.addEventListener('click', function() {
            banner.style.display = 'none';
            navbar.style.top = '0';
        });
    }
});
</script>